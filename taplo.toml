# taplo (TOML) formatting configuration. CI runs `taplo fmt --check`; run `taplo fmt` locally.
# Scope to our own TOML (the archived old/ demo and build dirs are intentionally left alone).
include = [
  "Cargo.toml",
  "crates/**/Cargo.toml",
  "deny.toml",
  "rust-toolchain.toml",
  "taplo.toml",
]

# Formatting only — no JSON-schema association/validation (keeps `fmt` offline & deterministic).
[schema]
enabled = false

[formatting]
reorder_keys = false
reorder_arrays = false
array_auto_collapse = true
array_auto_expand = true
compact_arrays = true
align_entries = false
